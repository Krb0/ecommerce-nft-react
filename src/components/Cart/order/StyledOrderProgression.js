import styled from "styled-components";

const StyledOrderProgression = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 0.2rem;
  margin-bottom: 0.5rem;
  .order-arrow {
    font-size: 30px;
  }
  svg {
    font-size: 40px;
  }
  span {
    font-size: 0.7rem;
  }
`;

export default StyledOrderProgression;
