import styled from "styled-components";

const StyledSorter = styled.select`
  margin: 0.5rem 10% 0 auto;
  border: 1px solid white;
  option {
    border: none;
    background-color: white;
  }
`;

export default StyledSorter;
